on: push

jobs:
  publish:
    name: Publish
    runs-on: self-hosted
    environment: Production
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      
      - name: Print out source contents
        run: ls ./

      - name: Print out destination contents
        run: cd ${{secrets.DEPLOY_PATH}} && ls
